### ✅ **2. Importance of `with` statement**

The `with` statement ensures files close automatically after use, preventing memory leakage and file corruption. It simplifies file handling and avoids manual closing.
Example: `with open("data.txt") as f:`